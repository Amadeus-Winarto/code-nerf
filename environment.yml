name: codenerf
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python>=3.8
  - pip
  - pip:
      - opencv-python
      - tensorboard
      - imageio
      - imageio-ffmpeg
      - lpips
  - scipy
  - numpy
  - matplotlib
  - pytorch
  - pytorch-cuda=11.8
  - torchvision
  - scikit-image
  - tqdm